---
description: TypeScript source conventions
globs: src/**/*.ts
---
<!-- generated by @starling-cloud/agents — edit fragments, not here -->
When editing TypeScript:

- Strict types; avoid `any` (cast through `unknown` only at real boundaries).
- Match the repo's module style; keep entry points (`entry.lib.ts`,
  `entry.app.ts`) thin.
- Run `npm run typecheck` and `npm run lint` after changes.
